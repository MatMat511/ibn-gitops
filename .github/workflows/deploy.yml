name: GitOps Pages

on:
  push:
    branches: [main]

jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Générer index.html depuis l’intention
        run: |
          VERSION=$(yq '.site.version' intent.yaml)
          HEADER=$(yq '.site.header' intent.yaml)

          cat <<EOF > index.html
          <html>
          <body>
            <h1>$HEADER</h1>
            <p>Version $VERSION</p>
          </body>
          </html>
          EOF
  schedule:
    - cron: "*/1 * * * *"
